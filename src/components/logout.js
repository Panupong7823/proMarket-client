import React from 'react';


const logout = ({ onClick }) => {
  return (
    <svg
      
      viewBox="0 0 24 24"
      width="24"
      height="24"
      fill="currentColor"
      onClick={onClick}
      style={{ cursor: 'pointer' }}
    >

    </svg>
  );
};

export default Icon;
